# Local env + Python venv
.venv
.env

# Backend (FastAPI) not needed for frontend build
backend/

# Scripts not needed for frontend
scripts/

# Local data files (DB, JSON, etc.)
data/
*.db

# Build artifacts & deps (Vercel will install fresh)
node_modules/
.next/
